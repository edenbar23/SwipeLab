# Integration API Endpoints

Base path: `/api/v1/system`

| Method | Endpoint | Description |
|---|---|---|
| `POST` | `/sync-stardbi` | Manually trigger a synchronization with Stardbi (SuperAdmin only) |

*(Note: Stardbi Mock endpoints under `/stardbi` are for local development only and are not part of the production API.)*
